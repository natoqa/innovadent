---
import { SITIO } from '../config/site';
import { urlAbsoluta } from '../lib/seo';
import '../styles/global.css';
// Mismo archivo que pide global.css: Vite lo emite una sola vez con el mismo hash.
import fuenteArchivo from '@fontsource-variable/archivo/files/archivo-latin-wdth-normal.woff2?url';

interface Props {
  titulo: string;
  descripcion?: string;
  /** Ruta canónica sin dominio, por ejemplo "/tratamientos/ortodoncia". */
  ruta?: string;
  noIndexar?: boolean;
}

const { titulo, descripcion = SITIO.descripcion, ruta, noIndexar = false } = Astro.props;

const tituloCompleto = titulo === SITIO.nombre ? titulo : `${titulo} — ${SITIO.nombre}`;
// El dominio vive en site.ts, no en astro.config: una sola fuente de verdad.
const canonical = urlAbsoluta(ruta ?? Astro.url.pathname);
---

<!doctype html>
<html lang="es-PE">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />

    <!-- Un solo archivo variable cubre todos los pesos y anchos del alfabeto
         latino. Se precarga para que el titular no espere a que llegue el CSS. -->
    <link rel="preload" href={fuenteArchivo} as="font" type="font/woff2" crossorigin />

    <title>{tituloCompleto}</title>
    <meta name="description" content={descripcion} />
    {canonical && <link rel="canonical" href={canonical} />}
    {noIndexar && <meta name="robots" content="noindex, nofollow" />}

    <meta property="og:type" content="website" />
    <meta property="og:site_name" content={SITIO.nombre} />
    <meta property="og:locale" content={SITIO.locale} />
    <meta property="og:title" content={tituloCompleto} />
    <meta property="og:description" content={descripcion} />
    {canonical && <meta property="og:url" content={canonical} />}

    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />

    <slot name="head" />
  </head>

  <body>
    <a class="saltar" href="#contenido">Saltar al contenido</a>

    <slot name="encabezado" />

    <main id="contenido">
      <slot />
    </main>

    <slot name="pie" />
  </body>
</html>

<style>
  .saltar {
    position: absolute;
    left: -9999px;
    top: 0;
    z-index: 100;
    padding: 0.75rem 1.25rem;
    min-height: 48px;
    display: inline-flex;
    align-items: center;
    background-color: var(--color-superficie);
    color: var(--color-tinta);
    border-radius: var(--radius-accion);
    box-shadow: inset 0 0 0 1px var(--color-borde);
  }

  .saltar:focus {
    left: 0.5rem;
    top: 0.5rem;
  }
</style>
